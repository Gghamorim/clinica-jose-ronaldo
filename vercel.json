{
  "version": 2,
  "buildCommand": "bun run build && if not exist api mkdir api && copy /Y dist\\server\\index.js api\\index.js > NUL",
  "installCommand": "bun install",
  "outputDirectory": "dist/client",

  "rewrites": [
    {
      "source": "/((?!assets/|favicon.ico|.*\\..*).*)",
      "destination": "/api/index.js"
    }
  ]
}