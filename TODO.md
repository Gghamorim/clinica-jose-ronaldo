## TODO
- [x] Remover runtime inválido de `functions` no `vercel.json`.
- [ ] Garantir que o commit corrigido foi enviado ao GitHub (push) e o Vercel vai clonar commit novo (não `e22ce03`).
- [ ] Deixar `vercel.json` sem bloco `functions` (mais seguro) e fazer novo commit + push.
- [ ] Fazer redeploy no Vercel e confirmar que o erro `Function Runtimes must have a valid version` sumiu.

